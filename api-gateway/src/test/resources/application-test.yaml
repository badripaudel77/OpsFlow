server:
  port: 8080

spring:
  application:
    name: api-gateway

  data:
    redis:
      host: localhost
      port: 6379

  cloud:
    gateway:
      routes: []

jwt:
  secret: test-secret-key-for-testing-must-be-at-least-256-bits-long-for-hs256

logging:
  level:
    root: WARN
